#import "../../99-索引与模板/TypstTemplate/computer-notes.typ": *


// ── 文档元信息 ──
#set document(
  title: "人工智能",
  author: "Violet",
  date: datetime.today(),
)

// 应用全局样式
#show: apply-style

// ══════════════════════════════════════════════════════════════════════
// 封面
// ══════════════════════════════════════════════════════════════════════

#make-cover(
  "人工智能",
  "Violet",
  date: datetime.today().display(),
)

// 目录
#make-outline(depth: 2)


// ─────────────────────────────────────────────────────────────────────
// Part 1：人工智能导论（基础知识）
// ─────────────────────────────────────────────────────────────────────
#part("人工智能导论")

// Chapter 1：人工智能概述 ✅
// 1.1 AI 定义与发展历程：图灵测试、AI 寒冬、深度学习革命
// 1.2 AI 的分类：弱 AI vs 强 AI、符号主义 vs 连接主义、行为主义
// 1.3 AI 的研究领域：计算机视觉、自然语言处理、语音识别、机器人学
// 1.4 AI 伦理与社会影响：偏见、隐私、就业、安全、可解释性
= 人工智能概述

// Chapter 2：数学基础 🔶
// 2.1 线性代数：向量、矩阵、特征值分解、SVD、谱定理
// 2.2 概率论与统计：概率分布、贝叶斯定理、最大似然估计、假设检验
// 2.3 微积分：梯度、偏导数、链式法则、泰勒展开、雅可比矩阵
// 2.4 优化理论：凸优化、拉格朗日乘子法、KKT 条件、梯度下降
= 数学基础

// Chapter 3：信息论基础 ⚪
// 3.1 熵与信息量：香农熵、联合熵、条件熵
// 3.2 互信息：点互信息、归一化互信息
// 3.3 KL 散度：相对熵、JS 散度、交叉熵
// 3.4 编码理论：霍夫曼编码、算术编码
= 信息论基础

// Chapter 4：搜索与推理 🔶
// 4.1 无信息搜索：BFS、DFS、迭代加深、双向搜索
// 4.2 启发式搜索：A* 算法、IDA*、贪心最佳优先
// 4.3 对抗搜索：Minimax、Alpha-Beta 剪枝、Monte Carlo Tree Search
// 4.4 约束满足问题（CSP）：回溯搜索、弧相容、向前检查
= 搜索与推理


// ─────────────────────────────────────────────────────────────────────
// Part 2：机器学习基础
// ─────────────────────────────────────────────────────────────────────
#part("机器学习基础")

// Chapter 1：机器学习概述 🔶
// 1.1 机器学习定义与分类：监督学习、无监督学习、半监督学习、强化学习
// 1.2 机器学习工作流程：数据准备、模型训练、评估、部署
// 1.3 过拟合与欠拟合：偏差-方差权衡、正则化
// 1.4 模型评估指标：准确率、精确率、召回率、F1、AUC-ROC
= 机器学习概述

== 过拟合与欠拟合

=== 核心概念
// 过拟合（Overfitting）：在训练集上表现优异，但在未见过的测试集上表现差
// - 模型学到了训练数据中的噪声和偶然模式，而非真实规律
// - 表现：训练 loss 低，验证 loss 高
// 欠拟合（Underfitting）：模型在训练集上就无法捕捉数据中的基本模式
// - 模型过于简单，不足以表达数据的真实分布
// - 表现：训练 loss 和验证 loss 都很高

#note[
  过拟合与欠拟合是模型训练中最常见的两个问题，理解它们的本质是调优模型的前提。
]

*过拟合（Overfitting）*：模型在训练集上表现优异，但在未见过的测试集上表现差。模型学到了训练数据中的噪声和偶然模式，而非真实规律。典型表现为训练 loss 低、验证 loss 高，训练准确率接近 $100%$ 而验证准确率远低于此。

*欠拟合（Underfitting）*：模型在训练集上就无法捕捉数据中的基本模式。模型过于简单，不足以表达数据的真实分布。典型表现为训练 loss 和验证 loss 都很高，模型无法收敛到一个好的解。

#tip[
  用一个直观的类比理解：过拟合 = 死记硬背（背下了习题答案但不会做新题），欠拟合 = 根本没学会（连课本知识都没掌握）。
]

=== 偏差-方差权衡（Bias-Variance Tradeoff）
// 偏差（Bias）：模型预测值与真实值之间的系统性偏差
// - 高偏差 → 模型过于简单 → 欠拟合
// 方差（Variance）：模型对不同训练集的敏感程度
// - 高方差 → 模型过于复杂 → 过拟合
// 总误差 = 偏差² + 方差 + 不可约噪声
// - 模型复杂度 vs 总误差曲线：存在最优复杂度点

偏差（Bias）衡量模型预测值与真实值之间的系统性偏差，反映模型的拟合能力。高偏差意味着模型过于简单，无法捕捉数据中的真实规律，导致欠拟合。

方差（Variance）衡量模型对不同训练集的敏感程度，反映模型的稳定性。高方差意味着模型过于复杂，对训练数据中的噪声过度敏感，导致过拟合。

对于平方损失函数，模型的期望泛化误差可以分解为：

$
  "Error" = "Bias"^2 + "Variance" + sigma^2
$

其中 $sigma^2$ 是不可约噪声（数据本身的随机性）。模型复杂度与总误差的关系呈现 U 形曲线：简单模型偏差高方差低（欠拟合），复杂模型偏差低方差高（过拟合），最优复杂度点位于两者之间。

#info[
  这一权衡是机器学习的核心概念：我们无法同时消除偏差和方差，只能在两者之间找到最优平衡点。
]

=== 检测方法
// 过拟合信号：训练 loss 持续下降，验证 loss 开始上升（早停点）
// - 训练准确率接近 100% 而验证准确率远低于此
// - 模型权重的值过大（L2 正则化可抑制）
// 欠拟合信号：训练 loss 始终处于高位，不再下降
// - 验证 loss 与训练 loss 接近，但都很高

*过拟合信号*：

- 训练 loss 持续下降，验证 loss 开始上升（出现早停点）
- 训练准确率接近 $100%$ 而验证准确率远低于此
- 模型权重的值过大（可通过 L2 正则化抑制）
- 模型在训练集上表现完美，但在新数据上泛化能力差

*欠拟合信号*：

- 训练 loss 始终处于高位，不再下降
- 验证 loss 与训练 loss 接近，但两者都很高
- 模型在训练集和测试集上都表现不佳

#caution[
  判断过拟合或欠拟合时，必须同时观察训练集和验证集的损失曲线。仅看训练 loss 无法区分两者——训练 loss 高一定是欠拟合，但训练 loss 低可能是正常拟合也可能是过拟合。
]

=== 缓解策略
// 增加训练数据：更多样本、数据增强
// 降低模型复杂度：减少层数/神经元、特征选择
// 正则化：L1/L2/Elastic Net（详见 Ch2.3）
// 早停法（Early Stopping）：监控验证 loss，在上升前停止
// Dropout：训练时随机失活部分神经元
// 集成学习：Bagging / Boosting 降低方差（详见 Ch3）

针对过拟合：

- *增加训练数据*：更多样本或通过数据增强（旋转、翻转、裁剪等）扩充训练集，让模型看到更多样的数据
- *降低模型复杂度*：减少网络层数、神经元数量，或进行特征选择，去掉冗余特征
- *正则化*：L1 正则化（稀疏化权重）、L2 正则化（权重衰减）、Elastic Net（两者结合），详见线性模型章节
- *早停法（Early Stopping）*：在训练过程中监控验证 loss，当验证 loss 开始上升时提前停止训练
- *Dropout*：训练时随机失活部分神经元，强迫网络学习更鲁棒的特征，减少神经元之间的共适应
- *集成学习*：Bagging 通过降低方差提升泛化能力，Boosting 通过降低偏差提升拟合能力

针对欠拟合：

- *增加模型复杂度*：增加网络层数、神经元数量，或使用更强大的模型架构
- *减少正则化强度*：降低 L1/L2 正则化系数，或暂时关闭 Dropout
- *增加训练时间*：让模型训练更多轮次，直到损失不再下降
- *特征工程*：引入更多相关特征，或构造多项式特征

== 模型评估指标

=== 混淆矩阵
// ┌─────┬───────────┬───────────┐
// │     │ 预测正类   │ 预测负类   │
// ├─────┼───────────┼───────────┤
// │真实正│ TP        │ FN        │
// │真实负│ FP        │ TN        │
// └─────┴───────────┴───────────┘
// TP（True Positive）：正确预测为正类
// TN（True Negative）：正确预测为负类
// FP（False Positive）：误报，实际为负但预测为正
// FN（False Negative）：漏报，实际为正但预测为负

混淆矩阵（Confusion Matrix）是分类模型评估的基础工具，它以矩阵形式展示模型预测结果与真实标签的对比。

#figure(
  table(
    columns: 3,
    align: center,
    table.header(
      [ ],
      [预测正类],
      [预测负类],
    ),
    [真实正类], [TP（真正类）], [FN（假负类）],
    [真实负类], [FP（假正类）], [TN（真负类）],
  ),
  caption: [二分类混淆矩阵],
) <fig:confusion-matrix>

四个基本统计量的含义：

- *TP（True Positive）*：实际为正，预测也为正——正确检出
- *TN（True Negative）*：实际为负，预测也为负——正确拒绝
- *FP（False Positive）*：实际为负，但预测为正——误报（Type I Error）
- *FN（False Negative）*：实际为正，但预测为负——漏报（Type II Error）

#tip[
  记忆技巧：第一个字母表示预测是否正确（T/F），第二个字母表示预测类别（P/N）。FP = 错误地预测为正类，FN = 错误地预测为负类。
]

=== 分类指标
// 准确率（Accuracy）= (TP + TN) / (TP + TN + FP + FN)
// - 简单直观，但在类别不平衡时失效（全部预测为多数类也能高准确率）
// 精确率（Precision）= TP / (TP + FP)
// - 关注"查准"：预测为正类中有多少是真正类
// - 适用场景：垃圾邮件过滤、推荐系统（宁缺毋滥）
// 召回率（Recall）= TP / (TP + FN)
// - 关注"查全"：真正类中有多少被正确检出
// - 适用场景：癌症筛查、欺诈检测（宁可错杀不可放过）
// F1-Score = 2 × Precision × Recall / (Precision + Recall)
// - 精确率与召回率的调和平均，均衡两者的指标
// Specificity（特异度）= TN / (TN + FP)
// AUC-ROC：ROC 曲线下的面积
// - 评估模型在不同阈值下的整体排序能力
// - 不受类别不平衡影响，适合排序任务

基于混淆矩阵可以计算出多个分类评估指标：

*准确率（Accuracy）*：

$
  "Accuracy" = (T P + T N) / (T P + T N + F P + F N)
$

最直观的指标，表示所有样本中被正确分类的比例。但在类别不平衡时会失效——例如 $99%$ 的样本为负类时，全部预测为负也能达到 $99%$ 准确率。

*精确率（Precision）*：

$
  "Precision" = T P / (T P + F P)
$

关注"查准"：预测为正类的样本中有多少是真正类。适用于误报代价高的场景，如垃圾邮件过滤（宁缺毋滥，不能把正常邮件误判为垃圾）。

*召回率（Recall）*：

$
  "Recall" = T P / (T P + F N)
$

关注"查全"：所有真实正类样本中有多少被正确检出。适用于漏报代价高的场景，如癌症筛查、欺诈检测（宁可错杀不可放过）。

*F1-Score*：

$
  F_1 = 2 dot "Precision" dot "Recall" / ("Precision" + "Recall")
$

精确率与召回率的调和平均数，兼顾两者。当精确率和召回率都很高时，F1 才高；其中一个很低时，F1 也会被拉低。

*特异度（Specificity）*：

$
  "Specificity" = T N / (T N + F P)
$

衡量模型对负类的识别能力，与召回率互补。在医学检测中，高特异度意味着健康人被误判为患病的概率低。

*AUC-ROC*：

ROC 曲线以假正率（FPR）为横轴、真正率（TPR，即召回率）为纵轴绘制。AUC（Area Under Curve）是 ROC 曲线下方的面积，取值范围 $[0, 1]$。AUC 越大，模型的排序能力越强。AUC 不受分类阈值影响，适合评估模型的整体区分能力。

#info[
  AUC 的概率含义：随机取一个正样本和一个负样本，模型给正样本的预测分数高于负样本的概率即为 AUC 值。AUC = 0.5 相当于随机猜测，AUC = 1.0 为完美分类器。
]

=== 多分类指标
// Macro 平均：每个类别分别计算指标后取算术平均
// - 平等对待每个类别，不考虑类别大小
// Micro 平均：全局统计 TP / FP / FN 后统一计算
// - 受大类别影响更大
// Weighted 平均：按每个类别的样本数加权平均

多分类问题需要将二分类指标扩展到多个类别，常用的聚合方式有三种：

*Macro 平均*：对每个类别分别计算指标（如 Precision、Recall），然后取算术平均。平等对待每个类别，不考虑类别样本数差异。适合关注少数类表现的场景。

*Micro 平均*：在所有类别上全局统计 TP、FP、FN，然后统一计算指标。等价于在每个样本上平等投票，受大类别影响更大。当类别不平衡且希望反映整体性能时使用。

*Weighted 平均*：按每个类别的样本数加权平均，兼顾类别大小和类别性能。

#caution[
  Macro 和 Micro 的选择取决于业务需求：如果每个类别同等重要（如稀有疾病检测），用 Macro；如果样本量大的类别更重要（如整体准确率），用 Micro。
]

=== 回归指标
// MSE（均方误差）= 1/n × Σ(yᵢ - ŷᵢ)²
// - 对异常值敏感（平方放大误差）
// RMSE（均方根误差）= √MSE
// - 与原始数据单位一致，更直观
// MAE（平均绝对误差）= 1/n × Σ|yᵢ - ŷᵢ|
// - 对异常值鲁棒
// R²（决定系数）= 1 - (残差平方和) / (总平方和)
// - 取值范围 (-∞, 1]，越接近 1 模型拟合越好
// MAPE（平均绝对百分比误差）= 1/n × Σ|(yᵢ - ŷᵢ) / yᵢ| × 100%
// - 以百分比衡量误差，直观但 yᵢ=0 时不可用

回归任务使用连续值评估指标，衡量预测值与真实值之间的差距：

*MSE（均方误差）*：

$
  "MSE" = 1/n sum_(i=1)^n (y_i - hat(y)_i)^2
$

对每个误差取平方后求平均。由于平方放大了大误差的影响，MSE 对异常值敏感。

*RMSE（均方根误差）*：

$
  "RMSE" = sqrt("MSE")
$

MSE 的平方根，与原始数据单位一致，比 MSE 更直观。例如房价预测中，RMSE = 5 万元比 MSE = 25 万元² 更容易理解。

*MAE（平均绝对误差）*：

$
  "MAE" = 1/n sum_(i=1)^n |y_i - hat(y)_i|
$

使用绝对值而非平方，对异常值更鲁棒。当数据中存在较多离群点时，MAE 比 MSE 更能反映典型误差水平。

*R²（决定系数）*：

$
  R^2 = 1 - (sum_(i=1)^n (y_i - hat(y)_i)^2) / (sum_(i=1)^n (y_i - overline(y))^2)
$

表示模型解释了目标变量多少比例的方差。$R^2 = 1$ 表示完美拟合，$R^2 = 0$ 表示模型不比简单取均值好，$R^2 < 0$ 表示模型比均值预测还差。

*MAPE（平均绝对百分比误差）*：

$
  "MAPE" = 1/n sum_(i=1)^n |(y_i - hat(y)_i) / y_i| dot 100%
$

以百分比形式衡量误差，直观且不受量纲影响。但当 $y_i = 0$ 时分母为零，无法计算。

#tip[
  回归指标选择经验：如果数据无明显异常值，RMSE 是首选；如果有异常值干扰，MAE 更鲁棒；如果需要向非技术人员解释模型性能，R² 或 MAPE 更直观。
]

=== 如何选择指标
// - 类别平衡 → Accuracy
// - 类别不平衡 → Precision / Recall / F1
// - 排序任务 → AUC-ROC
// - 回归任务 → RMSE（量纲一致） / MAE（鲁棒） / R²（解释力）

指标选择的核心原则是匹配业务需求：

#figure(
  table(
    columns: (auto, auto, auto),
    align: (left, left, left),
    table.header(
      [任务场景],
      [推荐指标],
      [原因],
    ),
    [类别平衡的分类], [Accuracy], [各类别样本数接近，准确率能反映整体性能],
    [类别不平衡的分类], [Precision / Recall / F1], [Accuracy 在不平衡数据下会失效],
    [需要排序的分类], [AUC-ROC], [评估模型在所有阈值下的综合排序能力],
    [无异常值的回归], [RMSE], [与原始数据单位一致，直观],
    [有异常值的回归], [MAE], [对离群点鲁棒],
    [需要解释的回归], [R² / MAPE], [百分比形式，非技术人员易理解],
  ),
  caption: [不同任务场景的指标选择指南],
)

#caution[
  没有万能指标。实际项目中通常同时观察多个指标，综合判断模型性能。例如医疗诊断中，需要同时关注 Recall（不能漏诊）和 Precision（不能过度误诊）。
]

// Chapter 2：线性模型 🔶
// 2.1 线性回归：最小二乘法、正规方程、梯度下降
// 2.2 逻辑回归：sigmoid 函数、交叉熵损失、多分类
// 2.3 正则化：L1（Lasso）、L2（Ridge）、Elastic Net
// 2.4 广义线性模型：softmax 回归、感知机
= 线性模型

== 线性回归

=== 最小二乘法
// 目标函数：最小化残差平方和 RSS = Σ(yᵢ - wᵀxᵢ)²
// 闭式解推导：ŵ = (XᵀX)⁻¹Xᵀy
// 几何解释：投影到列空间

线性回归（Linear Regression）是最简单、最经典的监督学习模型。它假设目标变量 $y$ 与特征向量 $x$ 之间存在线性关系：

$
  y = w^T x + b + epsilon
$

其中 $w$ 是权重向量，$b$ 是偏置（可合并进 $w$ 通过增广特征向量 $x=[x_1,dots,x_p,1]$ 处理），$epsilon$ 是随机误差项，通常假设 $epsilon tilde "Normal"(0, sigma^2)$。

最小二乘法（Ordinary Least Squares, OLS）通过最小化残差平方和（Residual Sum of Squares）来求解参数：

$
  "RSS"(w) = sum_i (y_i - w^T x_i)^2 = norm(y - X w)^2_2
$

#note[
  选择平方和而非绝对值和的直觉：平方函数可导，使得优化问题有闭式解；且高斯噪声下最小二乘等价于极大似然估计。
]

=== 正规方程
// 解析解 w* = (XᵀX)⁻¹Xᵀy
// 前提：XᵀX 可逆（特征数 < 样本数、无多重共线性）
// 复杂度 O(n²p)，小数据集适用
// 数值不稳定时改用伪逆或 SVD

将 RSS 对 $w$ 求导并令梯度为零，得到正规方程（Normal Equation）：

$
  (X^T X) w = X^T y
$

若 $X^T X$ 可逆（即满秩），则解析解为：

$
  hat(w) = (X^T X)^(-1) X^T y
$

#info[
  正规方程直接在一步内求得最优解，无需迭代。当特征数 $p$ 远小于样本数 $n$（通常 $p < 10^4$）时，正规方程比梯度下降更高效。
]

*使用条件*：

- $X^T X$ 必须可逆——要求特征数 $p$ 小于样本数 $n$，且特征之间无严格多重共线性
- 计算复杂度 $O(n^2 p)$，$n$ 很大时计算 $X^T X$ 的逆矩阵代价过高
- 当 $X^T X$ 接近奇异时，数值稳定性差，改用伪逆 $(X^T X)^+$ 或 SVD 分解求解

#caution[
  当 $p > n$ 时 $X^T X$ 不可逆，正规方程失效。此时需要正则化（Ridge / Lasso）或用梯度下降法迭代求解。
]

=== 梯度下降
// 批量梯度下降（BGD）：每步使用全部样本
// 随机梯度下降（SGD）：每步使用一个样本，收敛更快
// 小批量梯度下降（Mini-batch GD）：折中方案
// 学习率选择：太大震荡不收敛，太小收敛慢
// 收敛条件：梯度范数 < 阈值 / 固定轮次 / loss 不再下降

当数据量较大时，正规方程的计算成本过高。梯度下降（Gradient Descent）通过迭代方式逐步逼近最优解。

线性回归的 RSS 损失对 $w$ 的梯度为：

$
  nabla "RSS"(w) = -2 X^T (y - X w)
$

参数更新规则：$w ← w - eta * nabla "RSS"(w)$

根据每次更新使用的样本数，梯度下降有三种变体：

*批量梯度下降（Batch GD）*：每步使用全部 $n$ 个样本计算梯度。梯度方向准确，收敛稳定；但每步计算量大，不适合大数据集。

*随机梯度下降（Stochastic GD, SGD）*：每步随机选择一个样本 $(x_i, y_i)$ 计算梯度。计算量小、收敛速度快，能跳出局部极小点；但梯度有噪声，收敛路径震荡。

*小批量梯度下降（Mini-batch GD）*：每步使用 $m$ 个样本（通常 $m = 32"~"256$）计算梯度。折中 BGD 的稳定性和 SGD 的效率，是实际中最常用的版本。

#tip[
  Mini-batch 大小的选择：太小则梯度噪声大（接近 SGD），太大则计算效率下降（接近 BGD）。常用的 mini-batch 大小是 32、64、128，适配 GPU 的并行架构。
]

*学习率选择*：

- 学习率 $eta$ 控制每步的步长
- 太大：梯度下降在最优值附近震荡，甚至发散不收敛
- 太小：收敛速度极慢，需要大量迭代次数
- 实践中常用学习率调度策略：固定衰减、指数衰减、余弦退火

*收敛条件*：

- 梯度范数小于阈值 $norm(nabla L) < epsilon$
- 达到预设的最大迭代次数
- 连续多轮 loss 变化小于阈值

=== 模型评估
// 回归指标：MSE / RMSE / MAE / R²（详见 Ch1 模型评估指标）
// R² = 1 - RSS/TSS，解释方差比例
// F 检验：整体显著性检验

训练完成后，需要用评估指标衡量模型表现。回归模型的常见评估指标已在第一章详细介绍，这里补充线性回归特有的显著性检验。

对于线性回归，总平方和可以分解为回归平方和与残差平方和：

$
  "TSS" = sum (y_i - bar(y))^2 = "RSS" + "ESS"
$

其中 ESS（Explained Sum of Squares）是模型解释的方差部分。决定系数 $R^2$ 衡量了模型对目标变量的解释能力：

$
  R^2 = 1 - "RSS"/"TSS"
$

$R^2$ 的取值范围为 $[0, 1]$，越接近 $1$ 表示模型拟合效果越好。但 $R^2$ 随特征数增加只会上升不会下降，因此在高维场景下应使用*调整 $R^2$*：

$
  R^2_"adj" = 1 - ("RSS"/(n-p-1))/("TSS"/(n-1))
$

*F 检验*：用于检验回归模型整体的显著性，原假设为所有系数均为零（模型无预测能力）：

$
  F = ("ESS"/p)/("RSS"/(n-p-1)) ~ F(p, n-p-1)
$

若 F 统计量大于临界值，拒绝原假设，说明模型至少有一个特征显著影响目标变量。

== 逻辑回归

=== sigmoid 函数
// σ(z) = 1 / (1 + e^{-z})，输出 (0,1) 概率
// 决策边界：σ(wᵀx) ≥ 0.5 → 预测正类
// 概率解释：P(y=1|x) = σ(wᵀx)

逻辑回归（Logistic Regression）虽然名称中带有"回归"，但实则是用于*分类*问题的模型。它在线性回归的输出上套一层 sigmoid 函数，将输出映射到 $(0, 1)$ 区间，作为正类的概率估计。

sigmoid 函数定义：

$
  sigma(z) = 1 / (1 + e^(-z))
$

#note[
  sigmoid 函数具有三个重要性质：(1) 输出在 $(0,1)$ 之间，天然适合做概率；(2) 单调递增，保持线性决策面的单调性；(3) 在 $z = 0$ 附近近似线性，两端饱和——这既是优势（对极端值不敏感）也是劣势（梯度消失）。
]

逻辑回归模型假设：

$
  P(y=1 | x) = sigma(w^T x) = 1 / (1 + e^(-w^T x))
$

决策边界为 $w^T x = 0$（此时 $sigma(0) = 0.5$），是一个线性超平面。$w^T x >= 0$ 时预测为正类，否则为负类。

=== 交叉熵损失
// L(w) = -Σ [yᵢ log ŷᵢ + (1-yᵢ) log(1-ŷᵢ)]
// 为什么不用 MSE：非凸优化困难、概率解释不匹配
// 梯度：∇L = Σ (ŷᵢ - yᵢ)xᵢ

逻辑回归的参数通过最大化似然函数求解。给定数据集 ${(x_i, y_i)}$，$y_i in {0, 1}$，似然函数为：

$
  L(w) = product_(i) P(y_i | x_i) = product_(i) sigma(w^T x_i)^(y_i) dot (1 - sigma(w^T x_i))^(1 - y_i)
$

取负对数得到交叉熵损失（Cross-Entropy Loss）：

$
  "CE"(w) = - sum_i [y_i log sigma(w^T x_i) + (1 - y_i) log(1 - sigma(w^T x_i))]
$

交叉熵的梯度形式极其简洁，与线性回归的梯度形式上一致：

$
  nabla "CE"(w) = sum_i (sigma(w^T x_i) - y_i) x_i
$

#info[
  为什么逻辑回归不用 MSE？MSE 在逻辑回归中是非凸函数，梯度下降容易陷入局部极小点；而交叉熵是凸函数，保证梯度下降能找到全局最优解。
]

#tip[
  交叉熵的梯度 $nabla "CE" = (hat(y) - y)x$ 形式上与线性回归的梯度 $nabla "RSS" prop (hat(y) - y)x$ 一致——区别仅在于 $hat(y)$ 的计算方式：线性回归是恒等函数，逻辑回归是 sigmoid 函数。
]

=== 多分类
// One-vs-Rest（OvR）：K 个二分类器
// One-vs-One（OvO）：K(K-1)/2 个分类器，投票决策
// Softmax 回归（见 2.4）是更自然的多分类扩展

逻辑回归天然用于二分类。扩展到多分类有两种策略：

*One-vs-Rest（OvR）*：训练 $K$ 个二分类器，每个分类器 $f_k$ 区分"属于类别 $k$" vs "不属于类别 $k$"。预测时选择得分最高的类别。复杂度 $O(K)$，每类只需一个分类器。

*One-vs-One（OvO）*：每对类别 $(i, j)$ 训练一个分类器 $f_(i j)$，共 $K(K-1)/2$ 个。预测时所有分类器投票，得票最多的类别胜出。复杂度 $O(K^2)$，但每个分类器只使用两个类别的数据（适合类别数少、每类数据量大的场景）。

#caution[
  多分类逻辑回归存在"不一致"问题：OvR 中各类别的概率之和不一定为 1。如果要得到合理的概率估计，应使用下一节的 Softmax 回归（多项逻辑回归）。
]

== 正则化

=== L1 正则化（Lasso）
// 损失：RSS + λ Σ|wⱼ|
// 特点：产生稀疏解（部分权重为 0）→ 特征选择
// 解法：坐标下降法 / LARS

正则化通过在损失函数中加入参数惩罚项，控制模型复杂度以缓解过拟合。

Lasso（Least Absolute Shrinkage and Selection Operator）使用 $L_1$ 范数惩罚：

$
  L_"lasso"(w) = "RSS"(w) + lambda sum_j |w_j|
$

#note[
  Lasso 的关键特性：$L_1$ 惩罚的几何形状（菱形约束区域）使得最优解落在坐标轴上，产生*稀疏解*——部分权重恰好为零，自动实现特征选择。
]

- *优点*：自动特征选择，生成可解释的稀疏模型；适合高维数据（$p >> n$）
- *缺点*：当特征分组相关时，Lasso 从中仅随机选一个，不如 Elastic Net 稳定
- *求解算法*：坐标下降法（Coordinate Descent）和 LARS（Least Angle Regression）

=== L2 正则化（Ridge）
// 损失：RSS + λ Σwⱼ²
// 特点：权重缩小但不为 0，保持所有特征
// 闭式解：w* = (XᵀX + λI)⁻¹Xᵀy
// 解决 XᵀX 不可逆的问题

Ridge 回归（岭回归）使用 $L_2$ 范数惩罚：

$
  L_"ridge"(w) = "RSS"(w) + lambda sum_j w_j^2
$

Ridge 有优美的闭式解：

$
  hat(w)_"ridge" = (X^T X + lambda I)^(-1) X^T y
$

#info[
  $lambda I$ 的引入使得 $X^T X + lambda I$ 始终可逆——即使 $p > n$。从数值角度看，Ridge 在特征高度相关时提供稳定的解。
]

- *优点*：收缩权重但不置零，保留所有特征；矩阵始终可逆，数值稳定；适合特征间存在多重共线性的场景
- *缺点*：不能做特征选择，模型可解释性不如 Lasso

#tip[
  Ridge 通过控制 $lambda$ 的大小调节正则化强度。$lambda$ 越大，权重收缩越厉害，方差降低、偏差增大。最优 $lambda$ 通常通过交叉验证选择。
]

=== Elastic Net
// 损失：RSS + λ₁Σ|wⱼ| + λ₂Σwⱼ²
// 结合 L1 的稀疏性和 L2 的稳定性
// 适用于特征数远大于样本数、特征分组相关

Elastic Net 结合了 Lasso 和 Ridge 的惩罚项：

$
  L("en")(w) = "RSS"(w) + lambda_1 sum_j |w_j| + lambda_2 sum_j w_j^2
$

#note[
  Elastic Net 的设计动机是取 Lasso 和 Ridge 各自的优点：Lasso 做特征选择（稀疏解），Ridge 处理特征分组相关（平滑解）。
]

- 当特征数远大于样本数（$p >> n$）时，Lasso 最多只能选出 $n$ 个特征，Elastic Net 不受此限制
- 当特征间存在分组相关性时，Lasso 随机选一个，Elastic Net 同时选入一组相关特征
- 通过调节 $lambda_1/lambda_2$ 的比率，可以在稀疏性和平滑性之间灵活权衡

=== 正则化路径与调参
// 正则化路径：λ 从大到小，模型复杂度递增
// 调参方法：交叉验证选择 λ
// 偏差-方差视角：λ↑ → 方差↓ 偏差↑

*正则化路径*：展示不同 $lambda$ 值下各特征系数的变化曲线。$lambda$ 从大到小（从左到右），模型复杂度递增：
- 大 $lambda$：惩罚重，几乎所有系数被压到零（Lasso）或很小（Ridge）
- 小 $lambda$：惩罚轻，系数接近普通最小二乘解
- 最优 $lambda$ 通常位于路径中间段

#info[
  正则化路径图是诊断工具：如果某个特征系数在 $lambda$ 变化时剧烈波动，说明该特征不稳定，可能存在共线性问题。
]

*调参方法*：

- *交叉验证*（K-fold CV）：将数据分为 $K$ 折，对每个候选 $lambda$ 在 $K-1$ 折上训练、1 折上验证，取平均验证误差最小的 $lambda$
- *$lambda_"min"$ vs $lambda_"1se"$*：前者是验证误差最小的 $lambda$，后者是 $lambda_"min"$ 之一个标准误范围内最简单的模型（更大的 $lambda$）——按"一个标准误准则"更常用，因为它给出更稀疏/更稳定的模型

*偏差-方差视角*：

$
  lambda arrow(u) => "方差" arrow(d), "偏差" arrow(u)
$

正则化的本质是主动引入偏差（将系数向零收缩），换取方差的降低，使总预测误差减小。

== 广义线性模型

=== Softmax 回归
// 多分类的线性模型：P(y=k|x) = exp(wₖᵀx) / Σ exp(wⱼᵀx)
// 损失：交叉熵 + softmax
// 决策边界：线性（类间由超平面分隔）

Softmax 回归（又称多项逻辑回归）是逻辑回归在多分类上的直接推广。对于 $K$ 个类别，每个类别 $k$ 有独立的权重向量 $w_k$，样本 $x$ 属于类别 $k$ 的概率为：

$
  P(y=k | x) = exp(w_k^T x) / sum_(j=1)^K exp(w_j^T x), quad k=1,dots,K
$

#note[
  Softmax 函数将 $K$ 个线性输出 $w_k^T x$ 转换为和为 1 的概率分布。当 $K=2$ 时，Softmax 退化为 sigmoid 逻辑回归。
]

参数通过最小化交叉熵损失求解：

$
  L(w_1,dots,w_K) = - sum_i sum_k [y_i = k] log P(y_i = k | x_i)
$

其中 $[y_i = k]$ 是指示函数，当样本 $i$ 属于类别 $k$ 时为 1，否则为 0。

与逻辑回归类似，Softmax 回归的决策边界也是*线性超平面*——在相邻两类之间由 $w_i^T x = w_j^T x$ 决定。

#tip[
  Softmax 回归与 $K$ 个 OvR 逻辑回归的区别：Softmax 的 $K$ 组权重同时优化，保证概率和为 1；OvR 每个分类器独立训练，概率可能不一致。
]

=== 感知机
// 二分类线性模型：y = sign(wᵀx + b)
// 学习算法：误分类驱动更新 w ← w + yx
// 收敛定理：线性可分数据在有限步内收敛
// 局限性：只能处理线性可分、输出硬分类无概率

感知机（Perceptron）是神经网络和深度学习的前身，由 Rosenblatt 于 1958 年提出。它是最简单的线性二分类模型：

$
  y = "sign"(w^T x) = cases(+1 "if" w^T x >= 0, -1 "if" w^T x < 0)
$

*学习算法*：感知机采用误分类驱动的在线学习策略。

遍历训练集，当遇到误分类样本时进行更新：

- 若 $y_i = +1$ 但 $w^T x_i < 0$：$w arrow(l) w + eta x_i$
- 若 $y_i = -1$ 但 $w^T x_i >= 0$：$w arrow(l) w - eta x_i$

统一形式：$w arrow(l) w + eta y_i x_i$

感知机收敛定理：*如果训练数据线性可分，感知机算法在有限步内收敛*。

#caution[
  感知机的三个主要局限：(1) 只能处理线性可分数据；(2) 输出硬分类（$plus.minus 1$），不提供概率估计；(3) 即使数据线性可分，最终解也不唯一（取决于初始值和样本顺序）。这些局限催生了后来的支持向量机和多层神经网络。
]

#info[
  感知机的思想是现代神经网络的基本单元。一个单层感知机是一个线性分类器，多个感知机堆叠为多层感知机（MLP）则具有拟合非线性决策边界的能力。
]

// Chapter 3：决策树与集成学习 🔶
// 3.1 决策树：ID3、C4.5、CART、信息增益、基尼系数
// 3.2 随机森林：Bagging、特征随机性、OOB 评估
// 3.3 Gradient Boosting：AdaBoost、GBDT、XGBoost
// 3.4 LightGBM 与 CatBoost：直方图算法、叶子生长策略
// 3.5 Stacking 与 Blending：模型融合技术
= 决策树与集成学习

// Chapter 4：支持向量机（SVM）🔶
// 4.1 SVM 原理：最大间隔超平面、支持向量
// 4.2 核技巧：线性核、多项式核、RBF 核
// 4.3 软间隔与正则化：松弛变量、C 参数
// 4.4 SVM 扩展：SVR、One-Class SVM
= 支持向量机

// Chapter 5：聚类算法 🔶
// 5.1 K-Means：算法流程、K 值选择、K-Means++
// 5.2 层次聚类：凝聚法、分裂法、树状图
// 5.3 DBSCAN：密度聚类、核心点、边界点、噪声点
// 5.4 GMM 高斯混合模型：EM 算法、软聚类
// 5.5 聚类评估：轮廓系数、Calinski-Harabasz 指数
= 聚类算法

// Chapter 6：降维与流形学习 🔶
// 6.1 PCA 主成分分析：协方差矩阵、特征向量、累计方差贡献率
// 6.2 t-SNE：高维数据可视化、困惑度参数
// 6.3 UMAP：统一流形逼近、保留全局结构
// 6.4 LDA 线性判别分析：类间散度、类内散度
= 降维与流形学习

// Chapter 7：启发式优化算法 ⚪
// 7.1 遗传算法（GA）：选择、交叉、变异、适应度函数
// 7.2 模拟退火（SA）：Metropolis 准则、温度调度、冷却策略
// 7.3 粒子群优化（PSO）：个体最优、全局最优、速度更新
// 7.4 蚁群算法（ACO）：信息素更新、启发因子、路径选择
// 7.5 其他启发式算法：禁忌搜索、人工蜂群、差分进化
// 7.6 应用场景：组合优化、超参数调优、特征选择
= 启发式优化算法

// Chapter 8：概率图模型 ⚪
// 8.1 贝叶斯网络：有向图、条件独立性、推断算法
// 8.2 马尔可夫随机场：无向图、势函数、团
// 8.3 隐马尔可夫模型（HMM）：前向算法、Viterbi 算法
// 8.4 条件随机场（CRF）：序列标注、分词应用
= 概率图模型


// ─────────────────────────────────────────────────────────────────────
// Part 3：深度学习基础
// ─────────────────────────────────────────────────────────────────────
#part("深度学习基础")

// Chapter 1：神经网络基础 🔶
// 1.1 感知机与多层感知机（MLP）：神经元模型、激活函数
// 1.2 反向传播算法：链式法则、梯度计算、权重更新
// 1.3 激活函数：Sigmoid、Tanh、ReLU、Leaky ReLU、Swish
// 1.4 损失函数：MSE、交叉熵、Huber Loss、Focal Loss
// 1.5 优化器：SGD、Momentum、Adam、AdamW、LAMB
= 神经网络基础

// Chapter 2：深度学习框架 🔶
// 2.1 PyTorch 基础：Tensor、自动求导、nn.Module
// 2.2 PyTorch 数据加载：Dataset、DataLoader、transforms
// 2.3 模型训练循环：forward、backward、optimizer.step
// 2.4 模型保存与加载：state_dict、checkpoint
// 2.5 TensorBoard 可视化：损失曲线、计算图、 embeddings
= 深度学习框架

// Chapter 3：训练技巧与调优 🔶
// 3.1 权重初始化：Xavier、He、正交初始化
// 3.2 批量归一化（BatchNorm）：原理、实现、LayerNorm
// 3.3 Dropout 正则化：随机失活、Inverted Dropout
// 3.4 学习率调度：StepLR、Cosine Annealing、Warmup
// 3.5 早停法（Early Stopping）：验证集监控、patience
// 3.6 梯度裁剪：防止梯度爆炸、范数裁剪
= 训练技巧与调优

// Chapter 4：卷积神经网络（CNN）🔶
// 4.1 卷积操作：卷积核、步长、填充、输出尺寸计算
// 4.2 池化层：最大池化、平均池化、全局池化
// 4.3 经典 CNN 架构：LeNet、AlexNet、VGG、GoogLeNet
// 4.4 ResNet：残差连接、瓶颈结构、深度网络训练
// 4.5 现代 CNN：EfficientNet、ConvNeXt、MobileNet
= 卷积神经网络

// Chapter 5：循环神经网络（RNN）🔶
// 5.1 RNN 基础：时序建模、隐藏状态、BPTT
// 5.2 LSTM：遗忘门、输入门、输出门、细胞状态
// 5.3 GRU：简化 LSTM、更新门、重置门
// 5.4 双向 RNN：前向 + 后向、上下文信息
// 5.5 RNN 应用：语言模型、机器翻译、语音识别
= 循环神经网络（RNN）

// Chapter 6：Transformer 架构原理 🔶
// 6.1 Self-Attention 机制：Scaled Dot-Product、Multi-Head Attention
// 6.2 Positional Encoding：绝对位置编码、相对位置编码、RoPE
// 6.3 Encoder-Decoder 架构：Pre-LN、Post-LN、FFN
// 6.4 高效 Transformer：Linformer、Performer、Reformer、FlashAttention
= Transformer 架构原理


// ─────────────────────────────────────────────────────────────────────
// Part 4：计算机视觉
// ─────────────────────────────────────────────────────────────────────
#part("计算机视觉")

// Chapter 1：目标检测 🔶
// 1.1 目标检测概述：边界框、IoU、mAP、NMS
// 1.2 Two-Stage 检测器：R-CNN、Fast R-CNN、Faster R-CNN
// 1.3 One-Stage 检测器：YOLO 系列、SSD、RetinaNet
// 1.4 Anchor-Free 检测器：FCOS、CenterNet、DETR
// 1.5 检测优化：FPN、PANet、BiFPN
= 目标检测

// Chapter 2：图像分割 🔶
// 2.1 语义分割：FCN、U-Net、DeepLab 系列
// 2.2 实例分割：Mask R-CNN、SOLO、YOLACT
// 2.3 全景分割：Panoptic FPN
// 2.4 分割评估：IoU、Dice Coefficient、Pixel Accuracy
= 图像分割

// Chapter 3：图卷积网络（GCN）🔶
// 3.1 图数据基础：图结构表示、邻接矩阵、度矩阵、拉普拉斯矩阵
// 3.2 图卷积原理：谱图卷积（ChebNet、GCN）vs 空间图卷积（消息传递范式）
// 3.3 经典 GCN 模型：GCN、GraphSAGE、GAT（图注意力网络）
// 3.4 点云处理：PointNet、PointNet++、DGCNN
// 3.5 场景图生成：目标关系推理、Visual Genome 数据集
// 3.6 图结构在视觉推理中的应用：视觉问答、关系检测
= 图卷积网络（GCN）

// Chapter 4：视频理解 ⚪
// 4.1 视频分类：3D CNN、Two-Stream Network、SlowFast
// 4.2 动作检测：时空定位、Tubelet 检测
// 4.3 视频目标跟踪：Siamese Network、Transformer Tracker
// 4.4 视频生成：Video GAN、Diffusion Video
= 视频理解

// ─────────────────────────────────────────────────────────────────────
// Part 5：自然语言处理（NLP）
// ─────────────────────────────────────────────────────────────────────
#part("自然语言处理")

// Chapter 1：NLP 基础 🔶
// 1.1 文本预处理：分词、去停用词、词干提取、词形还原
// 1.2 词表示：One-Hot、TF-IDF、Word2Vec、GloVe、FastText
// 1.3 语言模型：N-Gram、神经语言模型、 perplexity
// 1.4 文本分类：情感分析、主题分类、意图识别
= NLP 基础

// Chapter 2：序列到序列模型 🔶
// 2.1 Encoder-Decoder 架构：编码、解码、注意力机制
// 2.2 注意力机制（Attention）：Scaled Dot-Product、Multi-Head
// 2.3 机器翻译：BLEU 评分、束搜索（Beam Search）
// 2.4 文本摘要：抽取式摘要、生成式摘要、ROUGE 评分
= 序列到序列模型

// Chapter 3：预训练语言模型 🔶
// 3.1 BERT：预训练任务（MLM、NSP）、微调策略
// 3.2 GPT 系列：自回归语言模型、提示工程、In-Context Learning
// 3.3 T5：Text-to-Text 框架、统一 NLP 任务
// 3.4 模型轻量化：DistilBERT、TinyBERT、ALBERT
// 3.5 高效推理：KV Cache、Speculative Decoding、量化
= 预训练语言模型

// Chapter 4：大语言模型（LLM）🔶
// 4.1 LLM 概述：规模定律、涌现能力、指令跟随
// 4.2 预训练语料：Common Crawl、The Pile、清洗与去重
// 4.3 监督微调（SFT）：指令微调、数据构建、损失函数（RLHF/DPO 见 Part 8 Ch4）
// 4.4 提示工程：Zero-Shot、Few-Shot、Chain-of-Thought
// 4.5 LLM 应用：问答系统、代码生成、Agent、RAG
= 大语言模型

// Chapter 5：信息抽取与知识图谱 ⚪
// 5.1 命名实体识别（NER）：BiLSTM-CRF、BERT-NER
// 5.2 关系抽取：远程监督、联合抽取、Graph Neural Network
// 5.3 知识图谱构建：实体链接、关系补全、知识融合
// 5.4 知识图谱应用：问答、推荐、推理
= 信息抽取与知识图谱


// ─────────────────────────────────────────────────────────────────────
// Part 6：强化学习
// ─────────────────────────────────────────────────────────────────────
#part("强化学习")

// Chapter 1：强化学习基础 🔶
// 1.1 RL 基本概念：Agent、Environment、State、Action、Reward
// 1.2 MDP 马尔可夫决策过程：状态转移、奖励函数、折扣因子
// 1.3 价值函数：状态价值 V(s)、动作价值 Q(s,a)
// 1.4 探索与利用：ε-greedy、UCB、Thompson Sampling
= 强化学习基础

// Chapter 2：表格方法 🔶
// 2.1 动态规划：策略迭代、价值迭代、贝尔曼方程
// 2.2 Monte Carlo 方法：首次访问、每次访问、重要性采样
// 2.3 Temporal Difference（TD）：TD(0)、Sarsa、Q-Learning
// 2.4 n-step TD：n-step Sarsa、n-step Q-Learning
= 表格方法

// Chapter 3：深度强化学习 🔶
// 3.1 DQN：经验回放、目标网络、Double DQN、Dueling DQN
// 3.2 Policy Gradient：REINFORCE、Actor-Critic、A2C
// 3.3 PPO： clipped surrogate objective、信任区域
// 3.4 SAC：最大熵 RL、软 Q-Learning、自动温度调节
// 3.5 DDPG/TD3：连续动作空间、确定性策略梯度
= 深度强化学习

// Chapter 4：高级强化学习 ⚪
// 4.1 Model-Based RL：世界模型、规划、MuZero
// 4.2 多智能体 RL：合作、竞争、MARL 算法
// 4.3 模仿学习：行为克隆、逆强化学习、GAIL
// 4.4 Meta-RL：元学习、快速适应、MAML
// 4.5 RL 应用：游戏 AI（AlphaGo、OpenAI Five）、机器人控制
= 高级强化学习


// ─────────────────────────────────────────────────────────────────────
// Part 7：AI 工程化与部署
// ─────────────────────────────────────────────────────────────────────
#part("AI 工程化与部署")

// Chapter 1：模型训练工程化 🔶
// 1.1 分布式训练：数据并行、模型并行、ZeRO
// 1.2 混合精度训练：FP16、BF16、AMP、损失缩放
// 1.3 实验管理：MLflow、Weights & Biases、TensorBoard
// 1.4 超参数优化：网格搜索、随机搜索、贝叶斯优化、Optuna
= 模型训练工程化

// Chapter 2：模型部署 🔶
// 2.1 模型导出：ONNX、TorchScript、SavedModel
// 2.2 推理引擎：TensorRT、OpenVINO、ONNX Runtime
// 2.3 服务化部署：Flask/FastAPI、TorchServe、Triton Inference Server
// 2.4 边缘部署：TensorFlow Lite、Core ML、NCNN、MNN
// 2.5 模型监控：延迟、吞吐量、错误率、漂移检测
= 模型部署

// Chapter 3：MLOps ⚪
// 3.1 数据版本控制：DVC、LakeFS
// 3.2 持续集成/持续部署：GitHub Actions、Jenkins、Kubeflow
// 3.3 实验追踪与注册：MLflow、Weights & Biases、Model Registry
// 3.4 模型压缩：量化、剪枝、知识蒸馏
// 3.5 模型治理：合规性、审计、公平性与偏见
= MLOps

// Chapter 4：云计算与 AI 平台 ⚪
// 4.1 GPU 云计算：AWS EC2、Azure VM、GCP Compute Engine
// 4.2 AI 平台：SageMaker、Vertex AI、Azure ML
// 4.3 容器化：Docker、Kubernetes、GPU 资源调度
// 4.4 无服务器推理：AWS Lambda、Cloud Functions
= 云计算与 AI 平台


// ─────────────────────────────────────────────────────────────────────
// Part 8：AI 治理与安全
// ─────────────────────────────────────────────────────────────────────
#part("AI 治理与安全")

// Chapter 1：可解释 AI 🔶
// 1.1 可解释性的重要性：黑盒问题、信任、监管
// 1.2 事后解释方法：LIME、SHAP、Grad-CAM、Integrated Gradients
// 1.3 内在可解释模型：决策树、规则列表、注意力可视化
= 可解释 AI

// Chapter 2：公平性与偏见 ⚪
// 2.1 群体公平与个体公平：统计均等、机会均等、反事实公平
// 2.2 偏见来源：数据偏见、算法偏见、部署偏见
// 2.3 去偏见技术：数据重采样、公平约束、对抗去偏
= 公平性与偏见

// Chapter 3：AI 安全 🔶
// 3.1 对抗样本：FGSM、PGD、对抗训练
// 3.2 数据投毒与后门攻击：脏标签、触发器
// 3.3 模型窃取与隐私：模型反转、成员推理、差分隐私
// 3.4 红队测试与安全评估：自动化红队、对抗性评估
= AI 安全

// Chapter 4：AI 对齐 ⚪
// 4.1 对齐问题概述：外延对齐与内涵对齐
// 4.2 RLHF 与 DPO：基于人类反馈的强化学习、直接偏好优化
// 4.3 宪法 AI：原则驱动的自我训练
// 4.4 可扩展监督：弱到强泛化、递归奖励建模
= AI 对齐


// ─────────────────────────────────────────────────────────────────────
// Part 9：Agent 理论与 AI 编程实践
// ─────────────────────────────────────────────────────────────────────
#part("Agent 理论与 AI 编程实践")

// Chapter 1：AI 编程工具概述 ⚪
// 1.1 Claude Code：Anthropic 的 AI 编程助手，原生支持 Agent-Skill 标准
// 1.2 Trae IDE：国产 AI 原生 IDE，规则系统与 MCP 集成
// 1.3 Kiro：AWS AI IDE，Spec 驱动开发与三阶段工作流
// 1.4 Cursor IDE：AI 代码编辑器，MDC 规则与链式 Agent 调用
// 1.5 GitHub Copilot：代码补全与生成，仓库级指令支持
= AI 编程工具概述

// Chapter 2：Agent-Skill 开放标准（行业事实标准）🔶
// 2.1 核心术语定义：Skill（最小可复用能力单元）、Agent（调度单元）、Command（用户入口）
// 2.2 目录结构标准：全局目录 ~/.anthropic/、项目目录 .claude/.trae/.kiro.claude/
// 2.3 SKILL.md 规范：YAML 元数据（name、description、version、tags）+ Markdown 正文
// 2.4 AGENTS.md 智能体调度标准：技能绑定、规划策略、约束规则
// 2.5 渐进式披露机制：Discovery → Activation → Execution 分层加载
// 2.6 作用域分层：全局级、项目级、文件级
= Agent-Skill 开放标准

// Chapter 3：MCP（Model Context Protocol）工具调用标准 🔶
// 3.1 MCP 定位：Agent 与本地工具的统一接口（USB-C 级通用协议）
// 3.2 三大核心能力：Tools（可执行操作）、Resources（读取文件）、Prompts（模板）
// 3.3 协议架构：JSON-RPC 2.0、客户端-主机-服务器模式
// 3.4 安全机制：最小权限原则、工具白名单、用户授权流程
// 3.5 各工具实现：.trae/mcp.json、Claude Code MCP 配置
= MCP（Model Context Protocol）工具调用标准

// Chapter 4：A2A（Agent-to-Agent）通信标准 ⚪
// 4.1 A2A 定位：Agent 间跨工具/跨平台通信协议
// 4.2 核心概念：AgentCard、消息格式、RPC 方法
// 4.3 应用场景：多智能体分工协作、任务委托、工件传递
// 4.4 多智能体模式：前端 Agent + 后端 Agent + 测试 Agent 协作
= A2A（Agent-to-Agent）通信标准

// Chapter 5：Rules 与 Spec 工作流标准 ⚪
// 5.1 Rules 标准：长期静态约束（编码规范、框架偏好）
// 5.2 加载模式：always（全局加载）、filematch（文件匹配）、manual（手动调用）
// 5.3 Spec 工作流（Kiro）：requirements.md → design.md → tasks.md 三段式
// 5.4 各工具规则实现对比：Trae 的 rules 嵌套、Cursor 的 MDC 格式
= Rules 与 Spec 工作流标准

// Chapter 6：Skill 开发实践 🔶
// 6.1 Skill 目录结构：SKILL.md、scripts/、references/、assets/
// 6.2 SKILL.md 设计原则：元数据精准化、指令分层、Token 优化
// 6.3 本地测试 Skill 设计：Maven 打包 → JAR 启动 → cURL 测试 → 进程清理
// 6.4 脚本封装：确定性任务 vs AI 生成任务、Shell/Python 脚本集成
// 6.5 技能分发：团队共享、版本控制、社区模板复用
